using System.Linq;
using UnityEngine;
using UnityEngine.SceneManagement;

//Loaded the game scene
//Only used prior to adding matchmaker; will be used again after adding lobby - not by end of final project
public class Startup : MonoBehaviour
{

    public string SampleScene;
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
